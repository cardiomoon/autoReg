# restore data with I() in column name

restore data with I() in column name

## Usage

``` r
restoreData2(df)
```

## Arguments

- df:

  An object of class "data.frame"

## Value

An object of class "data.frame"
