# takes the breaks as input and returns labels as output

takes the breaks as input and returns labels as output

## Usage

``` r
label_parse(breaks)
```

## Arguments

- breaks:

  character

## Value

a character vector
